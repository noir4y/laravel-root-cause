Review the exported `TraceEnvelope` and produce:

1. A concise root cause summary.
2. The minimum file set to inspect first.
3. The safest code change order.
4. A regression test plan before editing.

Prefer deterministic evidence from `signals`, `supporting_evidence`, and `repro`.
